# Use Amazon Linux 2
FROM public.ecr.aws/amazonlinux/amazonlinux:2

# Define build argument for the Python script URL
ARG WORKER_SCRIPT_URL

# 1. Install EPEL and all libraries required for tools
RUN amazon-linux-extras install epel -y && \
    yum update -y && \
    yum install -y \
    ImageMagick \
    mediainfo \
    perl-Image-ExifTool \
    python3 \
    python3-pip \
    python3-devel \
    gcc \
    gcc-c++ \
    make \
    cmake \
    wget \
    tar \
    xz \
    libmad \
    libid3tag \
    libsndfile \
    gd \
    boost-program-options \
    boost-filesystem \
    boost-regex \
    boost-system && \
    yum clean all

# 2. ImageMagick Fixes: Symlink and Security Policy for large sprites
RUN ln -s /usr/bin/convert /usr/bin/magick && \
    sed -i 's/policy domain="resource" name="disk" value="1GiB"/policy domain="resource" name="disk" value="10GiB"/' /etc/ImageMagick-6/policy.xml || true

# 3. Install audiowaveform via RPM
RUN wget -q https://codeberg.org/chrisn/audiowaveform/releases/download/1.10.1/audiowaveform-1.10.1-1.amzn2.x86_64.rpm && \
    yum localinstall -y audiowaveform-1.10.1-1.amzn2.x86_64.rpm && \
    rm audiowaveform-1.10.1-1.amzn2.x86_64.rpm && \
    ldconfig

# 4. Install Static FFmpeg & FFprobe
RUN wget -q https://johnvansickle.com/ffmpeg/releases/ffmpeg-release-amd64-static.tar.xz && \
    tar -xJf ffmpeg-release-amd64-static.tar.xz && \
    cp ffmpeg-*-amd64-static/ffmpeg /usr/bin/ffmpeg && \
    cp ffmpeg-*-amd64-static/ffprobe /usr/bin/ffprobe && \
    chmod +x /usr/bin/ffmpeg /usr/bin/ffprobe && \
    rm -rf ffmpeg-*-amd64-static*

# 5. Install Python AWS SDK (Boto3)
RUN pip3 install --no-cache-dir boto3

# 6. Setup Workspace and Script
WORKDIR /var/task
ENV PATH="/usr/bin:/usr/local/bin:${PATH}"

# Pull worker.py from the provided URL
RUN if [ -z "$WORKER_SCRIPT_URL" ]; then \
        echo "ERROR: WORKER_SCRIPT_URL build argument is missing." && exit 1; \
    fi && \
    wget -qO worker.py "$WORKER_SCRIPT_URL" && \
    chmod +x worker.py

# 7. Final Verification
RUN ffmpeg -version && audiowaveform --version && magick -version && exiftool -ver

# 8. ECS ENTRYPOINT (Runs script immediately)
ENTRYPOINT [ "/usr/bin/python3", "worker.py" ]
